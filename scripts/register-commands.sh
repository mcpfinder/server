#!/bin/bash

# Registration commands for viable MCP.so servers

node index.js register se --headless --use-uvx --description "duan-liClaude Model Context Protocol Weather Server Example" --tags "uvx,mcp-so"

node index.js register mcp-se --headless --use-uvx --description "phil65MCP (Model context protocol) server with LLMling backend" --tags "uvx,mcp-so"
